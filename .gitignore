# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Environment secrets
# .env.* 를 통째로 무시하되 커밋 대상인 .env.example만 예외로 둔다.
.env
.env.*
!.env.example

# Node (frontend tooling, if any)
node_modules/

# 프론트엔드 런타임 설정 — build-config.sh가 환경변수로부터 생성한다.
# 값 자체는 공개되어도 안전하지만(네이버 콘솔 리퍼러 화이트리스트로 보호) 환경별 분리를 위해 커밋하지 않는다.
frontend/config.js

# AED 스냅샷 — build_snapshot.py가 배포 빌드 시점에 생성한다 (docs/OFFLINE_DESIGN.md).
# 매일 갱신되는 ~9MB 파일이라 커밋하면 저장소가 1년에 1GB 가까이 불어난다.
frontend/data/

# 배포 도구 (Phase 6) — 프로젝트 ID 등 로컬 연결 정보가 들어간다
.vercel/
.wrangler/

# Supabase CLI 로컬 아티팩트
supabase/.temp/
supabase/.branches/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
.claude/

# Logs / sample data
docs/sample_response.json
*.log
